{
  "installCommand": "if [ -d flutter ]; then cd flutter && git pull && cd ..; else git clone https://github.com/flutter/flutter.git -b stable; fi",
  "buildCommand": "./flutter/bin/flutter build web",
  "outputDirectory": "build/web",
  "cleanUrls": true
}
